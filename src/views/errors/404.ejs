<%- include('../partials/header', { title: 'Página não encontrada - Cine-Resenhas' }) %>

<main>
    <div class="container text-center" style="padding-block: var(--esp-5); min-height: 60vh; display:flex; flex-direction:column; align-items:center; justify-content:center; gap: var(--esp-3);">
        <h1 style="font-size: 4rem; font-weight:800; color: var(--tertiary);">404</h1>
        <p style="font-size:1.2rem; font-weight:700;">Essa cena não existe no nosso catálogo.</p>
        <p style="color: var(--secondary); max-width:420px;">A página que você tentou acessar não foi encontrada. Ela pode ter sido removida ou o endereço está incorreto.</p>
        <a class="btn btn-primario" href="/">Voltar para o início</a>
    </div>
</main>

<%- include('../partials/footer', { js: '' }) %>
