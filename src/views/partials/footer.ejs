<footer>
    <p>© Cine-Resenhas</p>
</footer>

<script src="/js/main.js"></script>

<% if(typeof js !== "undefined" && js){ %>
    <% const scripts = Array.isArray(js) ? js : [js]; %>
    <% scripts.forEach(function(arquivo){ %>
        <script src="/js/<%= arquivo %>"></script>
    <% }) %>
<% } %>

</body>
</html>
